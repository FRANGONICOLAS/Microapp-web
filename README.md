# To Run application

## Use this command to find mySQL port.  

```
sudo lsof -i:3306
```

## Kill this process

```
sudo kill <PID>
```

## Run this command to use docker compose.

```
docker compose up --build
```
